<Window x:Class="StayHydrated.RestBreakWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"        
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        xmlns:local="clr-namespace:StayHydrated"
        mc:Ignorable="d"
        Title="FullScreen" Width="800" Height="600" WindowState="Maximized" ResizeMode="NoResize" Topmost="True" WindowStyle="None" ShowInTaskbar="False" AllowsTransparency="True">
    <Window.Background>
        <SolidColorBrush Color="#FF9C9A9A" Opacity="0.75"/>
    </Window.Background>    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <fa:ImageAwesome Icon="Spinner" Spin="True" SpinDuration="10" Grid.Row="1"  />
        <Button x:Name="CloseButton" Content="Close" Click="Button_Click" Margin="0,0,44,30" Grid.Row="2" Height="28" VerticalAlignment="Bottom" HorizontalAlignment="Right" Width="75" IsCancel="True"/>
    </Grid>
</Window>